#!/bin/sh

# This is a stub.
# This script should accept two arguments: username and password,
# then login using these credentials; currently no session is maintained
# and logging in is done at every call to MuxPi API scripts

exit 0
